using System;


namespace ConsoleApp31
{
    class Program
    {
        static void Main(string[] args)
        {
            int a, x1, x2;
            Console.WriteLine("Введите число a");
            a = Convert.ToInt32(Console.ReadLine());
            x1 = a / 100;
            a = (a % 100) * 10 + x1;
            Console.WriteLine("Число a после преобразований: " + a);
            Console.ReadKey();

        }
    }
}
